name: Trying for different shell

on:
   push:
      branches: [ main ]
      
jobs:
   run_windows_command:
      runs-on: windows-latest
      steps:
         - name: Bash command
           run: echo "hello"
           shell: bash
   
